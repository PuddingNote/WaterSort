# Unity generated folders
/[Ll]ibrary/
/[Tt]emp/
/[Oo]bj/
/[Bb]uild/
/[Bb]uilds/
/[Ll]ogs/
/[Uu]ser[Ss]ettings/
/[Mm]emoryCaptures/
/[Rr]ecordings/

# 안드로이드 빌드(IL2CPP/Gradle) 임시 네이티브 빌드 캐시(CMake/Ninja) — 실제로
# 빌드해보니 여기 있는 게 확인됨(2026-09-15). 매번 바뀌는 캐시 파일이라
# 이전에 실수로 git에 잡혀 있던 것도 이번에 같이 untrack했다.
/.utmp/

# Autogenerated Jetbrains Rider plugin
/[Aa]ssets/Plugins/Editor/JetBrains*

# Visual Studio / Rider / VS Code
.vs/
.gradle/
ExportedObj/
.consulo/
*.csproj
*.unityproj
*.sln
*.slnx
*.suo
*.tmp
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db
*.pidb.meta
*.pdb.meta
*.mdb.meta

sysinfo.txt

# Builds
*.apk
*.aab
*.unitypackage
*.app

crashlytics-build.properties

# Packed Addressables
/[Aa]ssets/[Aa]ddressable[Aa]ssets[Dd]ata/*/*.bin*
/[Aa]ssets/[Ss]treamingAssets/aa.meta
/[Aa]ssets/[Ss]treamingAssets/aa/*

# 릴리즈 서명 키스토어 — 절대 커밋 금지.
# 유출되면 앱을 사칭할 수 있고, 분실하면 앱을 영원히 업데이트할 수 없다.
*.keystore
*.jks

# 동적 TMP 폰트 아틀라스 — 에디터에서 플레이할 때마다 재생성되어 매번 바뀐 것처럼 보임.
# (Clear Dynamic Data On Build가 켜져 있으면 빌드 시 어차피 비워짐. 폰트 생성 설정 자체를
#  바꾼 경우에만 예외적으로 커밋할 것)
# 실제 경로가 정해지면 여기에 구체 경로를 추가한다. 예:
# /Assets/Fonts/*_Dynamic.asset

# OS files
.DS_Store
Thumbs.db
